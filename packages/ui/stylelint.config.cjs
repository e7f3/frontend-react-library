//eslint-disable-next-line @typescript-eslint/no-require-imports
const config = require('@library/build/stylelint.config.cjs');

/** @type {import('stylelint').Config} */
module.exports = {
    ...config,
};

